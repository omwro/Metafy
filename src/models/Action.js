export class Action {
    constructor(action, song, playlist, message) {
        this.action = action;
        this.song = song;
        this.playlist = playlist;
        this.message = message;
    }
}